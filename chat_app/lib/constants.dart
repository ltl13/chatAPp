import 'package:flutter/material.dart';

// Màu chủ đạo
const textBoxColor = Color(0xfff6f6f6);
const myWhiteColor = Color(0xffffffff);
const myPurpleColor = Color(0xff5b50dc);
const myBlackColor = Color(0xff222540);
const myGrayColor = Color(0xffacacac);
const myDefaultPadding = 20.0; // Padding mặc định
// Cái này là gradient, dùng cho text block trong chat screen
const myPrimaryGradient = LinearGradient(
  colors: [Color(0xff655ae4), Color(0xff463bc7)],
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
);
